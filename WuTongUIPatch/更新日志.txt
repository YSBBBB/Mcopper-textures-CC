3.5.3 ：
删除了部分多余的功能;
再次对 渲染锁住的漏斗矿车 功能做调整

3.5.2 :
修复了自定义功能的一些bug;
让自定义颜色对潜影盒GUI生效;
优化苦力怕预警的算法(爆炸预警响应更快,且基本杜绝误报);
修复了 渲染锁住的漏斗矿车 功能只在单人游戏生效的bug(但在多人游戏可能遇到一点问题(比如更新不及时之类的)？目前没发现);
在取消载入梧桐多彩UI材质包时知道把容器预览修复类型换为原版(如果之前是梧桐UI模式的话);
并在重新载入时切换为梧桐UI模式(如果之前是梧桐UI模式的话);
删除了一些不必要的构建版本;

3.5.1 :
在梧桐UI未载入时提供简略的酿造台小抄;
让小抄的按钮可被材质包自定义;
优化了小抄按钮的注入逻辑;
把大部分的AccessWidener的内容换成了AccessorMixin(不想频繁genSources)
对陷阱箱标题功能做了一些更改;
调整了一部分代码的布局;
让渲染锁住的漏斗矿车功能能脱离minihud运行;

3.5.0 :
修复了部分翻译键错误的问题;
让材质包可以主动适配容器预览（自定义模式）;
修复了马类实体的预览错误

3.4.2 :
将检测逻辑移动到逻辑线程上，不再处于渲染线程;
调整了物品栏标题的高度

3.4.1 :
处理了一些bug

3.4.0 :
添加了阻止玩家预览到自己背包的功能;
对于苦力怕预警，提高了检测间隔和扩大了检测范围;
让所有设置都在默认情况下对原版不做任何改变;
对语言文件的一些内容进行修改

3.3.2 :
优化了方法执行监测的性能开销;
优化了小抄的排版和按钮的位置;
修复了没有载入梧桐UI不能使用熔炉，酿造台预览进度的bug

3.3.1 :
处理了语言文件错别字

3.3.0 :
第3次优化苦力怕预警的逻辑:
用游戏刻代替帧数计时器，使计数更准确;
让4个模式单独运行互不影响;
防止多个渲染器调用时引发并发修改异常

3.2.0 :
再度优化苦力怕苦力怕预警的逻辑:
采用计数器赋值来代替原始的条件赋值，减少爆炸预警,高压预警误报的概率(即使误报也会在短时间内重置，几乎不存在影响)

3.1.0 :
优化苦力怕预警的逻辑，使报警更稳定，性能压力更小;
苦力怕预警添加了初级预警，危险预警，爆炸预警,高压预警4种形式

3.0.0 :
修复部分方块实体在打开时会崩溃的问题;
添加了苦力怕预警功能

2.9.0 :
重构部分代码，把有关是否加载材质包和语言的判断从ModUtils（感觉没有加s的必要啊）移到ResourcesLoadedListener;
该判断现在只在加载（重载）资源时执行一次，对性能的开销大大减少（之前一秒执行的次数等于游戏的帧数）;
小抄在熔炉类容器开启配方书时会自动收起，而且再点击按钮可以收起配方书显示小抄;
各种功能添加了对（原版）实体容器的支持

2.8.0 :
把屏幕标题的文本内容全部放进语言文件;
添加了渲染锁住的漏斗矿车的功能;
把陷阱箱GUI的标题与普通箱子做了区分

2.7.0 :
为小抄功能的开与关添加了自定义选项（会在对应的容器GUI上渲染按钮）

2.6.0 :
实现全语言加载（大）木桶、末影箱的GUI和酿造台的小抄（可关）

2.5.0 :
对容器标题显示模式的代码进行改进

2.4.0 :
重新引入梧桐的小抄功能(已经过棒冰大大许可);
通过实现REI的接口使其不再遮挡小抄

2.3.0 :
对代码进行重构，使渲染部分的代码不再臃肿

2.2.0 :
在棒冰大大的帮助下解决了2.0.0以来梧桐GUI的潜影盒预览颜色不准的问题(没错，2个版本就好了);
添加了为正常打开潜影盒的GUI绘制颜色的功能

2.1.0 :
修复了一些bug

2.0.0 ：
预览潜影盒时为潜影盒绘制单独的界面（可选）;
但仍有几个颜色调不准（暂时不想修改了......）

1.9.0 ：
添加了在预览熔炉（或酿造台）时显示进度的功能

1.8.0 ：（回收了1.1.0的伏笔）
为该模组自身单开了一栏设置界面

1.7.0 ：
用木桶的png图片判断是否载入了梧桐ui;
添加了不显示容器标题（或显示所有容器标题）的功能;
添加了不显示物品栏标题（或强制显示物品栏标题）的功能

1.6.0 ：
修复一些bug

1.5.2 ：
为（大）木桶和末影箱都叠加一层额外的装饰层（防止给它们改名时丢失材质）

1.5.1 ：（解决了1.4.0的问题）
再次尝试解决大木桶UI错乱问题（在客户端修改大木桶屏幕的标题），这样仅客户端安装可以正常使用

1.5.0 ：
绘制大木桶打开时的UI叠加装饰层

1.4.0 ：
尝试解决大木桶UI错乱问题（修改大木桶的默认名称），此方法需要服务端支持

1.3.0 ：
让个别的容器可以在被预览时绘制自己的GUI，而不是使用通用的GUI

1.2.0 ：
尝试让容器预览正确调用梧桐UI材质（使用mixin）

1.1.0 ：
添加了功能的开关（通过mixin集成在malilib配置界面下，可能有兼容性问题，之后可能单开一栏配置界面）

1.0.0 ：
重构了代码，提高了兼容性（由于功能较少，故兼容性最强）

0.1.0 ：
实现了最基础的功能（让容器预览默认调用原版材质，防止了绘制错乱问题）